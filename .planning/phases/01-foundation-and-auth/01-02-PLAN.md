---
phase: 01-foundation-and-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/app.dart
  - lib/core/theme/app_theme.dart
  - lib/core/router/app_router.dart
  - lib/features/shell/app_shell.dart
  - lib/features/today/today_screen.dart
  - lib/features/vault/vault_screen.dart
  - lib/features/settings/settings_screen.dart
  - pubspec.yaml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "App launches and displays a bottom navigation bar with Today, Vault, and Settings tabs"
    - "Tapping each tab navigates to the correct screen"
    - "Theme uses Sora font and amber/gold color scheme on light backgrounds"
    - "App uses Material 3 NavigationBar (not legacy BottomNavigationBar)"
  artifacts:
    - path: "lib/main.dart"
      provides: "App entry point with ProviderScope and ConvexClient initialization"
      min_lines: 10
    - path: "lib/app.dart"
      provides: "MaterialApp.router with theme and router config"
      min_lines: 15
    - path: "lib/core/theme/app_theme.dart"
      provides: "ThemeData with Sora font and amber ColorScheme"
      contains: "GoogleFonts.soraTextTheme"
    - path: "lib/core/router/app_router.dart"
      provides: "GoRouter with ShellRoute for bottom nav tabs"
      exports: ["router"]
    - path: "lib/features/shell/app_shell.dart"
      provides: "Scaffold with NavigationBar (Today/Vault/Settings)"
      contains: "NavigationBar"
    - path: "lib/features/today/today_screen.dart"
      provides: "Placeholder Today screen"
    - path: "lib/features/vault/vault_screen.dart"
      provides: "Placeholder Vault screen"
    - path: "lib/features/settings/settings_screen.dart"
      provides: "Placeholder Settings screen"
  key_links:
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "runApp(ProviderScope(child: App()))"
      pattern: "ProviderScope"
    - from: "lib/app.dart"
      to: "lib/core/router/app_router.dart"
      via: "MaterialApp.router uses GoRouter instance"
      pattern: "MaterialApp\\.router"
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/shell/app_shell.dart"
      via: "ShellRoute wraps tab screens in AppShell"
      pattern: "ShellRoute"
---

<objective>
Restructure the Flutter app from a monolithic main.dart into a feature-based architecture with Material 3 theming (Sora font, amber/gold color scheme) and bottom navigation shell.

Purpose: This establishes the app shell, navigation, and design system that every subsequent phase builds on. The feature-based directory structure enables parallel development in later phases.

Output: A running Flutter app with three-tab bottom navigation (Today/Vault/Settings), Sora font throughout, amber/gold color scheme, and placeholder screens ready to receive real content.
</objective>

<execution_context>
@C:/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and create theme + app entry point</name>
  <files>
    pubspec.yaml
    lib/main.dart
    lib/app.dart
    lib/core/theme/app_theme.dart
  </files>
  <action>
1. **Add required dependencies to `pubspec.yaml`:**
   ```bash
   flutter pub add google_fonts go_router riverpod flutter_riverpod
   ```
   Note: `google_sign_in`, `convex_flutter`, `flutter_secure_storage`, and `oauth2_client` are added in Plan 03/04 when they are actually used. This keeps this plan focused on shell + theme.

2. **Create `lib/core/theme/app_theme.dart`** — the design system:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:google_fonts/google_fonts.dart';

   /// Builds the app's Material 3 theme with Sora font and amber/gold color scheme.
   /// Per user decision: light + clean aesthetic, white/light gray backgrounds, modern feel.
   /// Amber/gold accent ties to the "vault" concept.
   ThemeData buildAppTheme() {
     final base = ThemeData(
       useMaterial3: true,
       colorScheme: ColorScheme.fromSeed(
         seedColor: const Color(0xFFFFC107), // amber[500]
         brightness: Brightness.light,
       ),
     );

     return base.copyWith(
       textTheme: GoogleFonts.soraTextTheme(base.textTheme),
       scaffoldBackgroundColor: Colors.white,
     );
   }
   ```

3. **Replace `lib/main.dart`** with the new entry point. The old notes app code is fully replaced:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'app.dart';

   void main() {
     WidgetsFlutterBinding.ensureInitialized();
     runApp(
       const ProviderScope(
         child: App(),
       ),
     );
   }
   ```

4. **Create `lib/app.dart`** — the MaterialApp.router:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'core/theme/app_theme.dart';
   import 'core/router/app_router.dart';

   class App extends ConsumerWidget {
     const App({super.key});

     @override
     Widget build(BuildContext context, WidgetRef ref) {
       final router = ref.watch(routerProvider);

       return MaterialApp.router(
         title: 'Intern Growth Vault',
         theme: buildAppTheme(),
         routerConfig: router,
         debugShowCheckedModeBanner: false,
       );
     }
   }
   ```

**Per user decisions:**
- Sora font applied globally via `GoogleFonts.soraTextTheme()` — NOT system font
- Amber/gold from `ColorScheme.fromSeed(seedColor: Color(0xFFFFC107))` — NOT a dark theme
- Material 3 enabled (`useMaterial3: true`) — required for NavigationBar
- No Cupertino components — switching to pure Material 3 (per research: use NavigationBar not BottomNavigationBar)

**Do NOT:**
- Keep any code from the old notes app — it is fully replaced
- Use `.SF Pro Text` system font — user locked Sora
- Use `BottomNavigationBar` — that is the legacy M2 widget
- Add dark theme — user chose light + clean aesthetic
  </action>
  <verify>
    <automated>cd "C:/Users/micah/OneDrive/Desktop/intern_tracker" && flutter pub get && dart analyze lib/main.dart lib/app.dart lib/core/theme/app_theme.dart 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `pubspec.yaml` includes google_fonts, go_router, riverpod, flutter_riverpod
    - `lib/main.dart` is the clean entry point with ProviderScope
    - `lib/app.dart` creates MaterialApp.router with Sora theme and amber color scheme
    - `lib/core/theme/app_theme.dart` defines buildAppTheme() with ColorScheme.fromSeed
    - `dart analyze` passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create router, navigation shell, and placeholder screens</name>
  <files>
    lib/core/router/app_router.dart
    lib/features/shell/app_shell.dart
    lib/features/today/today_screen.dart
    lib/features/vault/vault_screen.dart
    lib/features/settings/settings_screen.dart
  </files>
  <action>
1. **Create `lib/core/router/app_router.dart`** — GoRouter with ShellRoute:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:go_router/go_router.dart';
   import 'package:riverpod/riverpod.dart';
   import '../../features/shell/app_shell.dart';
   import '../../features/today/today_screen.dart';
   import '../../features/vault/vault_screen.dart';
   import '../../features/settings/settings_screen.dart';

   final routerProvider = Provider<GoRouter>((ref) {
     return GoRouter(
       initialLocation: '/today',
       routes: [
         // Auth route will be added in Plan 03
         ShellRoute(
           builder: (context, state, child) => AppShell(child: child),
           routes: [
             GoRoute(
               path: '/today',
               pageBuilder: (context, state) => const NoTransitionPage(
                 child: TodayScreen(),
               ),
             ),
             GoRoute(
               path: '/vault',
               pageBuilder: (context, state) => const NoTransitionPage(
                 child: VaultScreen(),
               ),
             ),
             GoRoute(
               path: '/settings',
               pageBuilder: (context, state) => const NoTransitionPage(
                 child: SettingsScreen(),
               ),
             ),
           ],
         ),
       ],
     );
   });
   ```
   Use `NoTransitionPage` for tab switches — no animation when switching bottom nav tabs (standard mobile pattern). The auth redirect guard is NOT added here — that comes in Plan 03 when auth state exists.

2. **Create `lib/features/shell/app_shell.dart`** — Bottom navigation scaffold:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:go_router/go_router.dart';

   class AppShell extends StatelessWidget {
     final Widget child;
     const AppShell({super.key, required this.child});

     static int _calculateSelectedIndex(BuildContext context) {
       final location = GoRouterState.of(context).matchedLocation;
       if (location.startsWith('/today')) return 0;
       if (location.startsWith('/vault')) return 1;
       if (location.startsWith('/settings')) return 2;
       return 0;
     }

     void _onDestinationSelected(BuildContext context, int index) {
       switch (index) {
         case 0: context.go('/today');
         case 1: context.go('/vault');
         case 2: context.go('/settings');
       }
     }

     @override
     Widget build(BuildContext context) {
       final selectedIndex = _calculateSelectedIndex(context);
       return Scaffold(
         body: child,
         bottomNavigationBar: NavigationBar(
           selectedIndex: selectedIndex,
           onDestinationSelected: (index) => _onDestinationSelected(context, index),
           destinations: const [
             NavigationDestination(
               icon: Icon(Icons.today_outlined),
               selectedIcon: Icon(Icons.today),
               label: 'Today',
             ),
             NavigationDestination(
               icon: Icon(Icons.lock_outlined),
               selectedIcon: Icon(Icons.lock),
               label: 'Vault',
             ),
             NavigationDestination(
               icon: Icon(Icons.settings_outlined),
               selectedIcon: Icon(Icons.settings),
               label: 'Settings',
             ),
           ],
         ),
       );
     }
   }
   ```
   Use `NavigationBar` (Material 3), NOT `BottomNavigationBar` (M2 legacy). Icon choices: `today` for daily canvas, `lock` for vault concept, `settings` for settings.

3. **Create placeholder screens** — each with centered text indicating the screen name. These are stubs for later phases to build on:

   `lib/features/today/today_screen.dart`:
   ```dart
   import 'package:flutter/material.dart';

   class TodayScreen extends StatelessWidget {
     const TodayScreen({super.key});

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('Today')),
         body: const Center(child: Text('Daily Canvas — coming in Phase 2')),
       );
     }
   }
   ```

   `lib/features/vault/vault_screen.dart`:
   ```dart
   import 'package:flutter/material.dart';

   class VaultScreen extends StatelessWidget {
     const VaultScreen({super.key});

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('Vault')),
         body: const Center(child: Text('Vault — coming in Phase 6')),
       );
     }
   }
   ```

   `lib/features/settings/settings_screen.dart`:
   ```dart
   import 'package:flutter/material.dart';

   class SettingsScreen extends StatelessWidget {
     const SettingsScreen({super.key});

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('Settings')),
         body: const Center(child: Text('Settings — coming soon')),
       );
     }
   }
   ```

4. **Delete old test file** `test/widget_test.dart` — it tests the old NotesApp which no longer exists. Tests for the new architecture will be added alongside feature implementation.

**Per user decisions:**
- Bottom nav tabs: Today / Vault / Settings (exactly these names)
- Today tab is default landing (initialLocation: '/today')
- Lock icon for Vault (ties to "vault" brand concept)
- No onboarding or setup wizard screens

**Do NOT:**
- Add auth guard to router yet — that is Plan 03's responsibility
- Add sign-in screen here — Plan 03 creates it
- Use StatefulWidget for AppShell — GoRouter handles tab state via URL, no local state needed
- Add detailed content to placeholder screens — these are Phase 2+ concerns
  </action>
  <verify>
    <automated>cd "C:/Users/micah/OneDrive/Desktop/intern_tracker" && dart analyze lib/ 2>&1 | tail -10 && flutter build apk --debug --target-platform android-arm64 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `lib/core/router/app_router.dart` defines GoRouter with ShellRoute wrapping three tab routes
    - `lib/features/shell/app_shell.dart` renders NavigationBar with Today/Vault/Settings tabs
    - Three placeholder screens exist at `lib/features/today/today_screen.dart`, `lib/features/vault/vault_screen.dart`, `lib/features/settings/settings_screen.dart`
    - `dart analyze` passes with no errors
    - App builds successfully
    - Tapping tabs navigates between screens
  </done>
</task>

</tasks>

<verification>
- App launches and shows Today screen with bottom navigation bar
- Tapping Vault tab shows vault placeholder, Settings tab shows settings placeholder
- Sora font is visible in all text elements
- Amber/gold color scheme visible in AppBar and NavigationBar
- `dart analyze lib/` reports no errors
- App builds for at least one target platform
</verification>

<success_criteria>
Flutter app runs with a three-tab bottom navigation shell (Today/Vault/Settings), Sora font, amber/gold Material 3 theme, and clean feature-based directory structure ready for auth and feature implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md`
</output>
