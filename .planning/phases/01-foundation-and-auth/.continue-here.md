---
phase: 01-foundation-and-auth
task: UAT verification
total_tasks: 10 UAT tests
status: blocked
last_updated: 2026-03-03T19:40:53.317Z
---

<current_state>
Phase 01 execution is COMPLETE (all 4 plans executed, VERIFICATION.md passed, ROADMAP updated).
We are now in `/gsd:verify-work 1` — UAT testing on device. The UAT file is created with 10 tests, but Test 1 is blocked because the app won't get past the splash screen.

The Android emulator (Medium Phone API 36.1, emulator-5554) is running. The debug APK was built and installed via adb. The app launches but hangs on the native splash screen — never renders Flutter UI.

Root cause (not yet fixed): `await initConvexClient()` in `main()` line 17 blocks before `runApp()`. The `ConvexClient.initialize()` call from `convex_flutter` package is likely using an incorrect API (the executor may have hallucinated the API surface). The logs show "Skipped 1007 frames!" confirming the main thread is blocked.
</current_state>

<completed_work>

- Plan 01-01: Convex backend + OIDC auth config — DONE (commit 704e948)
- Plan 01-02: Flutter app shell + bottom nav — DONE (commits 5151002, 6774d1f)
- Plan 01-03: Google sign-in flow + Convex bridge — DONE (commits 2f46645, e385f57), checkpoint approved
- Plan 01-04: Settings screen + Calendar + GitHub — DONE (commit 96fab02), checkpoint approved
- Phase verification: PASSED (01-VERIFICATION.md created, ROADMAP marked complete)
- Credentials configured: Web Client ID, Convex URL (grand-tortoise-682), GitHub OAuth (Ov23likLf45h9uWzEz3N)
- Android package renamed to com.internvault.app
- Convex backend set up at C:\Users\micah\OneDrive\Desktop\intern_vault_backend
- UAT file created: .planning/phases/01-foundation-and-auth/01-UAT.md (10 tests, 0 completed)
</completed_work>

<remaining_work>

- FIX BLOCKER: App stuck on splash screen due to `initConvexClient()` hang in main.dart
  - Investigate `convex_flutter` package API — check if `ConvexClient.initialize()` with `ConvexConfig` is the correct API
  - If API is wrong, fix convex_service.dart to use the actual convex_flutter API
  - If API is correct but hanging on network, wrap with timeout or defer initialization
- Run all 10 UAT tests (01-UAT.md) after the app launches successfully
- Complete verify-work workflow (diagnosis + fix planning if issues found)
</remaining_work>

<decisions_made>

- Web Client ID: 559229937063-sp4cfdk9dn3uano0g84f6ei502j7tvh7.apps.googleusercontent.com (set in auth_repository.dart and convex/auth.config.ts)
- Convex deployment: grand-tortoise-682 (not benevolent-antelope-462 which was auto-created)
- Convex backend location: C:\Users\micah\OneDrive\Desktop\intern_vault_backend (separate from Flutter project)
- Android package: com.internvault.app
- GitHub OAuth Client ID: Ov23likLf45h9uWzEz3N, callback: com.internvault.app://oauth2redirect
- Stale convex/ directory in Flutter project is a cleanup candidate (points to old deployment)
</decisions_made>

<blockers>
- APP STARTUP HANG: `ConvexClient.initialize()` in convex_service.dart is blocking main(). The `convex_flutter` package API may have been hallucinated by the executor. Need to read the actual package source to determine the correct initialization API. Check: `flutter pub cache` for convex_flutter source, or read pubspec.lock for version and check pub.dev docs.
</blockers>

<context>
The phase execution went smoothly — all 4 plans completed with checkpoints approved. The issue surfaced only during UAT when we tried to actually run the app on the emulator. The convex_flutter package (v3.0.1) API for initialization needs to be verified against the actual package source. The executor likely guessed at the API since convex_flutter is a relatively niche package.

Key files to investigate:
- lib/main.dart (line 17: await initConvexClient())
- lib/core/convex/convex_service.dart (ConvexClient.initialize with ConvexConfig)
- The actual convex_flutter package source in pub cache

The emulator is already running. Flutter binary is at /c/src/flutter/bin/flutter. ADB is at /c/Users/micah/AppData/Local/Android/Sdk/platform-tools/adb.exe.
</context>

<next_action>
1. Find convex_flutter package source: look in Flutter pub cache for the actual API
2. Fix convex_service.dart to use the correct convex_flutter initialization API
3. If convex_flutter doesn't exist or is wrong, consider using convex HTTP client directly or removing the Convex init from main() temporarily
4. Rebuild, reinstall on emulator, and resume UAT testing from Test 1
5. Continue /gsd:verify-work 1 with the existing 01-UAT.md file
</next_action>
